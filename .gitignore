# ───────────────────────────────────────────────
#  GitHub에는 '글쓰기거울'만 올립니다.
#  먼저 모든 것을 제외하고, 글쓰기거울 파일만 다시 포함합니다.
# ───────────────────────────────────────────────
/*

# ↓ 글쓰기거울 프로젝트만 포함
!/.gitignore
!/README.md
!/server.ps1
!/apikey.txt.example
!/cloudflare-worker.js
!/app/

# ↓ 쉬는시간 게임: 타자 연습 (낱말 비)
!/typing-rain/

# ↓ 어린이 체스 교실
!/chess/
!/chess-online-worker.js

# ↓ 다람쌤 오목 대회 플랫폼
!/omok/

# ↓ 우리 반 아이디어 보드 (와우아이디어스 대체, Cloudflare Worker + D1)
!/ideaboard-app/
!/ideaboard-worker.template.js
!/ideaboard-worker.js
!/build-ideaboard-worker.ps1
!/ideaboard-server.ps1

# ↓ 매일 어휘 30 (AI 어휘 학습, Cloudflare Worker + D1 + Gemini)
!/vocab-app/
!/vocab-worker.template.js
!/vocab-worker.js
!/build-vocab-worker.ps1
!/vocab-server.ps1

# ↓ 매일 영단어 30 (AI 영단어 학습, Cloudflare Worker + D1 + Gemini)
!/engword-app/
!/engword-worker.template.js
!/engword-worker.js
!/build-engword-worker.ps1
!/engword-server.ps1
!/engword-ai-proxy/
# wrangler 로컬 캐시(계정 정보)는 빼고
/engword-ai-proxy/.wrangler/

# ↓ 수담(數談) 음성 입력 (Level 0 오프라인 + Node 서버 + Cloudflare Worker)
!/sudam/

# ↓ 선생님 도구상자 (교사 업무 프롬프트 10종, Cloudflare Worker + Gemini)
!/promptbox-app/
!/promptbox-worker.template.js
!/promptbox-worker.js
!/build-promptbox.ps1
!/promptbox-server.ps1

# ───────────────────────────────────────────────
#  안전장치: 아래는 절대 올리지 않음 (비밀·개인정보)
# ───────────────────────────────────────────────
apikey.txt
data/
