# ───────────────────────────────────────────────
#  GitHub에는 '글쓰기거울'만 올립니다.
#  먼저 모든 것을 제외하고, 글쓰기거울 파일만 다시 포함합니다.
# ───────────────────────────────────────────────
/*

# ↓ 글쓰기거울 프로젝트만 포함
!/.gitignore
!/README.md
!/server.ps1
!/apikey.txt.example
!/cloudflare-worker.js
!/app/

# ↓ 쉬는시간 게임: 타자 연습 (낱말 비)
!/typing-rain/

# ↓ 어린이 체스 교실
!/chess/
!/chess-online-worker.js

# ↓ 다람쌤 오목 대회 플랫폼
!/omok/

# ↓ 우리 반 아이디어 보드 (와우아이디어스 대체, Cloudflare Worker + D1)
!/ideaboard-app/
!/ideaboard-worker.template.js
!/ideaboard-worker.js
!/build-ideaboard-worker.ps1
!/ideaboard-server.ps1

# ↓ 안전교육 문구 복사기 (주간학습안내용, Cloudflare Worker 자동배포)
!/safety-edu/

# ↓ 학급은행 (적금·대출·기부·주식, Cloudflare Worker + D1)
!/bank-app/
!/bank-worker.template.js
!/bank-worker.js
!/build-bank-worker.ps1

# ───────────────────────────────────────────────
#  안전장치: 아래는 절대 올리지 않음 (비밀·개인정보)
# ───────────────────────────────────────────────
apikey.txt
data/
